def squareList(list):
    #write some code that takes the list of numbers and squares them saving each into newList
    newList=[]
    return newList

number = 2


def basicMaths():
    # write some code that takes the global variable number and adds 4 then times by 10

    
    return number

def basicSelection(age):
    # write some code below that, dependent on the value of age will return "Old enough to drive if 17+ and too young otherwise

